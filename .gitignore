# dépendances
/node_modules

# build Next.js
/.next
/out
next-env.d.ts

# variables d'environnement (ne JAMAIS committer)
.env
.env.local
.env*.local

# divers
.DS_Store
*.tsbuildinfo
/.vercel

# Ajouté par `vercel link`. Le modèle, lui, doit rester suivi : c'est la seule
# documentation des variables attendues.
.env*
!.env.example

# Compétences agent installées par les intégrations Marketplace (Resend…).
# 580 Ko de documentation tierce, régénérable : on suit skills-lock.json,
# pas son contenu.
/.agents
/.claude
.vercel
